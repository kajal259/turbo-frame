<% frame_id = dom_id(model, "#{attribute}_turbo_frame") %>
<%= form_with(model: model, class: "contents", data: { turbo_frame: frame_id}) do %>
  <%= turbo_frame_tag frame_id, class: "inline-edit" do %>
    <%= yield %>
    <%= link_to edit_polymorphic_path(model) do%>
      Edit<%= model.class.human_attribute_name(attribute)%>
    <% end %>
  <% end %>
<% end %>

